<Window
    x:Class="RobotControllerApp.UnityDebugWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RobotControllerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource Brush.Background.Page}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0"
                Background="{StaticResource Brush.Background.Card}"
                BorderBrush="#1AFFFFFF" BorderThickness="0,0,0,1"
                Padding="16,10"
                x:Name="TitleBarDragArea">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Stretch" Background="Transparent">
                <FontIcon Glyph="&#xE8B2;" FontSize="18" Foreground="{StaticResource Brush.Primary}" VerticalAlignment="Center"/>
                <TextBlock Text="UNITY SCENE DATA STATE" FontSize="13" FontWeight="Bold"
                           Foreground="{StaticResource Brush.Primary}"
                           VerticalAlignment="Center" CharacterSpacing="60"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Padding="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Spacing="12" Margin="0,0,16,0">
                <TextBlock Text="ACTIVE MESSAGE STREAMS" FontSize="11" FontWeight="Bold" Opacity="0.5" CharacterSpacing="50"/>
                <ListView x:Name="MessageTypesList" SelectionChanged="MessageTypesList_SelectionChanged" 
                          Background="Transparent" BorderThickness="0"
                          ItemContainerStyle="{StaticResource ListViewItemExpanded}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:MessageState">
                            <Border Background="#0AFFFFFF" CornerRadius="6" Padding="14,10" Margin="0,0,0,6" BorderBrush="#1AFFFFFF" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Type, Mode=OneWay}" FontWeight="SemiBold" FontSize="14" Foreground="{x:Bind DirectionColor, Mode=OneWay}"/>
                                        <TextBlock Text="{x:Bind LastUpdated, Mode=OneWay}" FontSize="11" Opacity="0.6" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <Border Grid.Column="1" Background="#15FFFFFF" CornerRadius="12" Padding="10,4" VerticalAlignment="Center" BorderThickness="1" BorderBrush="#30FFFFFF">
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <FontIcon Glyph="&#xE895;" FontSize="10" Foreground="{x:Bind DirectionColor, Mode=OneWay}" VerticalAlignment="Center"/>
                                            <TextBlock Text="{x:Bind Count, Mode=OneWay}" FontSize="12" FontWeight="Bold" Foreground="{x:Bind DirectionColor, Mode=OneWay}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <Border Grid.Column="1" Background="#08FFFFFF" CornerRadius="8" BorderBrush="#1AFFFFFF" BorderThickness="1" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,16">
                        <FontIcon Glyph="&#xE943;" FontSize="16" Foreground="{StaticResource Brush.Primary}"/>
                        <TextBlock Text="LATEST PAYLOAD DETAIL" FontSize="11" FontWeight="Bold" Opacity="0.5" CharacterSpacing="50" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <ScrollViewer Grid.Row="1">
                        <TextBlock x:Name="PayloadDetailText" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" IsTextSelectionEnabled="True" Foreground="#D0D0D0" LineHeight="18"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
